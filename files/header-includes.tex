% header-includes.tex
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{anyfontsize}
\usepackage{atbegshi}
\usepackage{fontspec}
\usepackage{parskip}  % Add parskip package to handle spacing and indentation

% Set the main document font to Arial and size to 12
\setmainfont{Arial}
\fontsize{12}{14}\selectfont

% Use atbegshi to remove the first page
\AtBeginDocument{\AtBeginShipoutNext{\AtBeginShipoutDiscard}}

% Remove paragraph indentation
\setlength{\parindent}{0pt}
% Add space between paragraphs
\setlength{\parskip}{1em}

% Set up fancyhdr
\pagestyle{fancy}
\fancyhf{}
\setlength{\headheight}{52pt}  % Adjust to avoid warnings
